<UserControl
    x:Class="Bim4Everyone.RevitFiltration.Controls.Views.CategoriesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:vms="clr-namespace:Bim4Everyone.RevitFiltration.Controls.ViewModels"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300"
    x:ClassModifier="internal"
    d:DataContext="{d:DesignInstance vms:DynamicCategoriesFilterViewModel, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="../Resources/CustomStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <ui:ToggleSwitch
            Grid.Row="1"
            IsChecked="{Binding HideUnselectedCategories, UpdateSourceTrigger=PropertyChanged}"
            Content="Скрыть неотмеченные категории" />

        <DockPanel
            Grid.Row="2"
            LastChildFill="True">
            <ui:TextBox
                Margin="0 8"
                DockPanel.Dock="Top"
                Text="{Binding CategoriesFilter, UpdateSourceTrigger=PropertyChanged}"
                PlaceholderText="Поиск" />
            <ui:DataGrid
                SelectionMode="Single"
                AutoGenerateColumns="False"
                CanUserDeleteRows="False"
                CanUserResizeColumns="False"
                CanUserReorderColumns="False"
                ItemsSource="{Binding Categories.View, UpdateSourceTrigger=PropertyChanged}">
                <ui:DataGrid.Resources>
                    <Style
                        TargetType="CheckBox"
                        BasedOn="{StaticResource CustomColumnCheckBoxStyle}" />
                </ui:DataGrid.Resources>
                <ui:DataGrid.Columns>
                    <DataGridCheckBoxColumn
                        Width="47"
                        CanUserSort="False"
                        Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}">
                        <DataGridCheckBoxColumn.Header>
                            <CheckBox
                                IsChecked="{Binding DataContext.AllCategoriesSelected, RelativeSource={RelativeSource AncestorType=ui:DataGrid}, UpdateSourceTrigger=PropertyChanged}" />
                        </DataGridCheckBoxColumn.Header>
                    </DataGridCheckBoxColumn>
                    <DataGridTextColumn
                        Width="Auto"
                        IsReadOnly="True"
                        Binding="{Binding Name}"
                        Header="Название" />
                </ui:DataGrid.Columns>
            </ui:DataGrid>
        </DockPanel>
    </Grid>
</UserControl>