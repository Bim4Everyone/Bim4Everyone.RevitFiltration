<UserControl
    x:Class="Bim4Everyone.RevitFiltration.Controls.Views.RuleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vms="clr-namespace:Bim4Everyone.RevitFiltration.Controls.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300"
    x:ClassModifier="internal"
    d:DataContext="{d:DesignInstance vms:RuleViewModel, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="pack://application:,,,/Wpf.Ui;component/Controls/ComboBox/ComboBox.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="*" />
            <ColumnDefinition
                Width="100" />
            <ColumnDefinition
                Width="*" />
            <ColumnDefinition
                Width="Auto" />
        </Grid.ColumnDefinitions>

        <ComboBox
            Grid.Column="0"
            Width="200"
            Margin="3"
            IsEditable="False"
            DisplayMemberPath="Name"
            ItemsSource="{Binding CategoriesInfo.AvailableParams}"
            SelectedValue="{Binding SelectedParameter, UpdateSourceTrigger=PropertyChanged}" />

        <ComboBox
            Grid.Column="1"
            Margin="3"
            Width="100"
            IsEditable="False"
            DisplayMemberPath="Name"
            ItemsSource="{Binding AvailableOperators}"
            SelectedValue="{Binding SelectedOperator, UpdateSourceTrigger=PropertyChanged}" />

        <ComboBox
            Grid.Column="2"
            Margin="3"
            ScrollViewer.VerticalScrollBarVisibility="Visible"
            Text="{Binding StringValue}"
            ItemsSource="{Binding Values}"
            SelectedItem="{Binding SelectedValue, UpdateSourceTrigger=PropertyChanged}">
            <ComboBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel
                        IsVirtualizing="True"
                        VirtualizingStackPanel.VirtualizationMode="Recycling" />
                </ItemsPanelTemplate>
            </ComboBox.ItemsPanel>
            <ComboBox.ItemTemplate>
                <DataTemplate
                    DataType="{x:Type vms:ParamValueViewModel}">
                    <TextBlock
                        Text="{Binding ParamValue.DisplayValue}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
            <ComboBox.Style>
                <Style
                    BasedOn="{StaticResource DefaultComboBoxStyle}"
                    TargetType="{x:Type ComboBox}">
                    <Style.Triggers>
                        <DataTrigger
                            Value="True"
                            Binding="{Binding IsValueEditable}">
                            <Setter
                                Property="IsEditable"
                                Value="True" />
                        </DataTrigger>
                        <DataTrigger
                            Binding="{Binding IsValueEditable}"
                            Value="False">
                            <Setter
                                Property="IsEditable"
                                Value="False" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ComboBox.Style>
        </ComboBox>

        <ui:Button
            Grid.Column="3"
            Margin="3"
            Appearance="Danger"
            Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBox}, Mode=FindAncestor}, Path=DataContext.RemoveRuleCommand}"
            CommandParameter="{Binding}"
            Content="{ui:SymbolIcon Symbol=Dismiss20}" />
    </Grid>
</UserControl>